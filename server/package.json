{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx tsc",
    "start": "node dist/index.js",
    "prebuild": "rm -rf dist",
    "start:dev": "npx nodemon dist/index.js",
    "clean": "rm -rf node_modules and dist",
    "dev": "npx ts-node-dev --transpile-only --no-notify --exit-child index.ts",
    "generate": "ts-node --transpile-only src/graphql/schema.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^3.15.2",
    "apollo-server": "^3.12.0",
    "dotenv": "^16.1.4",
    "express": "^4.18.2",
    "graphql": "^15.8.0",
    "knex": "^2.4.2",
    "nexus": "^1.3.0",
    "pg": "^8.11.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.17",
    "@types/node": "^20.2.5",
    "nodemon": "^2.0.22",
    "prisma": "^3.15.2",
    "typescript": "^5.1.3"
  }
}
